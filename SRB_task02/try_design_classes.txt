Модули:
	1. Модуль парсера 
	2. Модуль сохранения и загрузки хмм 
	3. Модуль вывода ввода инфы пользователю

	4*. Тесты на каждый модуль

Задачи каждго модуля:

1. Моуль парсера (Саня)

	Входное:
		Файл .h

	Чё сдеалть:
		Разобрать по костачкам
		Сохранить с помошью класов в память
		Обработать ошибки во время прасинга

	Что останется после работы:
		Весь .h в памяти
		Удобная сисема классов для пользования других

2. Модуль сохранения (Дима)

	Входное:
		Классы после парсера
		Путь до сохраняемого файла от МЕНЮ

	Чё деалет:
		создать .json
		Заполнить его в нужном формате
		Сохранить в нужную папку

	Что остаётся после работы:
		---

3. Модуль ввода вывода (Тёма)
	
	Входной: 
		Пользователь пишет команды в меню
		Пользователь пишет .h файлик

	Чё сделать:
		Проверить что ввёл пользователь
		Пнуть нужную функцию по команде пользователя
		Красиво выводить информацию

	Что остаётся после работы:
		работате всегда

/* main file */

class Object()
	name 	
	key_word
	line_num
	file_name


class Define(Object)
	defined_word


class Function(Object)
	arg_type


/* import main file */

class Parser
	public:
		parse_file()
		вернуть массив объектов()

	privat:
		разбери строку
		убери пустые строки
		обработай строку
		и тд

		масиив Object

/* модуль чтения из файла */
class Read_file 
	открывает файлики
	читает файлики


/* модуль ввода вывода */
import argparce

class Menu
	кто пришёл 
	сделай это 

class Terminal_reader
	просто arg[arce]

